# ============================================================
# 필요한 사전 작업:
#   - Docker Hub 계정 및 레포지토리 생성
#   - EC2 인스턴스 생성 및 SSH 키 준비
#   - .env.prod 파일 작성
#   - GitHub Secrets 등록 (DOCKERHUB_USERNAME, EC2_HOST 등)
# ============================================================

# version: "3.9"

# services:
#   # 프론트엔드 (Nginx로 빌드된 정적 파일 서빙)
#   frontend:
#     image: ${DOCKERHUB_USERNAME}/frontend:latest
#     ports:
#       - "80:80"
#     networks:
#       - app-network
#     depends_on:
#       - backend

#   # 백엔드 (Gunicorn으로 프로덕션 서빙)
#   backend:
#     image: ${DOCKERHUB_USERNAME}/backend:latest
#     ports:
#       - "8000:8000"
#     env_file:
#       - .env.prod
#     command: gunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers 3
#     networks:
#       - app-network
#     depends_on:
#       - redis

#   # Redis (로컬과 동일한 설정)
#   redis:
#     image: redis:7-alpine
#     ports:
#       - "6379:6379"
#     volumes:
#       - redis_data:/data
#     networks:
#       - app-network

# volumes:
#   redis_data:

# networks:
#   app-network:
#     driver: bridge
